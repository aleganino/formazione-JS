// Modifica il codice solo sotto questa riga
class Vegetable {
  constructor(name) {
    this.name=name;
  }
}

// Modifica il codice solo sopra questa riga

const carrot = new Vegetable('carrot');
console.log(carrot.name); // Dovrebbe mostrare 'carrot'